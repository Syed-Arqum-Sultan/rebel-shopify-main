{%- liquid
  assign bs = block.settings
  assign dialog_id = 'FrameGuide-' | append: section.id | append: '-' | append: block.id
-%}

<div
  class="atelier-frame-guide spacing-style"
  style="{% render 'spacing-style', settings: block.settings %}"
  {{ block.shopify_attributes }}
>
  <button
    type="button"
    class="button button-secondary atelier-frame-guide__trigger"
    aria-haspopup="dialog"
    aria-controls="{{ dialog_id }}"
  >
    {{ bs.trigger_label | escape }}
  </button>

  <dialog
    id="{{ dialog_id }}"
    class="atelier-frame-guide__dialog color-{{ settings.popover_color_scheme }}"
    aria-labelledby="{{ dialog_id }}-title"
  >
    <div class="atelier-frame-guide__dialog-inner">
      <button
        type="button"
        class="atelier-frame-guide__close button-unstyled"
        aria-label="{{ 'accessibility.close_dialog' | t }}"
        data-close-dialog
      >
        <span class="svg-wrapper" aria-hidden="true">{{ 'icon-close.svg' | inline_asset_content }}</span>
      </button>
      {% if bs.heading != blank %}
        <h2 id="{{ dialog_id }}-title" class="atelier-frame-guide__title">
          {{ bs.heading | escape }}
        </h2>
      {% endif %}
      {% if bs.body != blank %}
        <div class="rte rte--inherit atelier-frame-guide__body">
          {{ bs.body }}
        </div>
      {% endif %}
    </div>
  </dialog>
</div>

<script>
  (function () {
    const root = document.currentScript.previousElementSibling;
    if (!root) return;
    const dialog = root.querySelector('dialog');
    const openBtn = root.querySelector('.atelier-frame-guide__trigger');
    const closeBtn = root.querySelector('[data-close-dialog]');
    if (!dialog || !openBtn) return;
    openBtn.addEventListener('click', () => {
      dialog.showModal();
    });
    closeBtn?.addEventListener('click', () => dialog.close());
    dialog.addEventListener('click', (e) => {
      const rect = dialog.getBoundingClientRect();
      const inDialog =
        rect.top <= e.clientY &&
        e.clientY <= rect.top + rect.height &&
        rect.left <= e.clientX &&
        e.clientX <= rect.left + rect.width;
      if (!inDialog) dialog.close();
    });
  })();
</script>

{% stylesheet %}
  .atelier-frame-guide__trigger {
    width: fit-content;
    font-size: 9px;
    letter-spacing: 2px;
    text-transform: uppercase;
    border-radius: 0;
  }

  .atelier-frame-guide__dialog {
    max-width: min(480px, 94vw);
    width: 100%;
    padding: 0;
    border: 1px solid rgba(201,169,110,0.18);
    border-radius: 0;
    background: var(--color-background);
    color: var(--color-foreground);
    box-shadow: 0 24px 64px rgba(0,0,0,0.5);
  }

  .atelier-frame-guide__dialog::backdrop {
    background: rgba(14,12,10,0.88);
  }

  .atelier-frame-guide__dialog-inner {
    position: relative;
    padding: 36px;
  }

  .atelier-frame-guide__close {
    position: absolute;
    top: 16px;
    right: 16px;
    padding: var(--padding-xs);
    cursor: pointer;
    color: inherit;
    transition: color 0.2s;
  }

  .atelier-frame-guide__title {
    margin: 0 0 6px;
    font-family: var(--font-heading--family);
    font-size: 26px;
    font-weight: 300;
  }

  .atelier-frame-guide__body {
    max-height: min(60vh, 28rem);
    overflow: auto;
    font-size: 11px;
    line-height: 1.8;
  }

  .atelier-frame-guide__body table {
    width: 100%;
    border-collapse: collapse;
    font-size: 11px;
  }

  .atelier-frame-guide__body table th {
    text-align: left;
    padding: 8px 10px;
    font-size: 8px;
    letter-spacing: 3px;
    text-transform: uppercase;
    border-bottom: 1px solid rgba(201,169,110,0.18);
    font-weight: 500;
  }

  .atelier-frame-guide__body table td {
    padding: 10px 10px;
    border-bottom: 1px solid rgba(201,169,110,0.08);
  }

  .atelier-frame-guide__body table tr:last-child td {
    border-bottom: none;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Frame size guide",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "trigger_label",
      "label": "Button label",
      "default": "Frame size guide"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Popup heading",
      "default": "Find your fit"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Guide content",
      "default": "<p>Measure your favorite frames or use our chart: narrow (below 130mm), medium (130–139mm), wide (140mm+). Our opticians can help finalize lens height and bridge fit.</p>"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Frame size guide"
    }
  ]
}
{% endschema %}
